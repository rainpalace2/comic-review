<div class="container"></div>
 <div class="row">
  <div class="col">
    <div class="text-center mt-5">
      <%= form_with url: search_path, method: :get, local: true do |f| %>
      <%= f.text_field :title, placeholder: "作品名・作者名で検索", class: "w-25" %>
      <%= f.submit "検索", class: "btn btn-primary" %>
      <% end %>
     </div>
    </div>
  </div>
</div>